# Changing the default prefix
unbind C-b
unbind C-Space
set -g prefix C-Space
bind C-Space send-prefix

# Splitting windows (opens in the same directory as the current on)
unbind %
unbind '"'

bind | split-window -h -c "#{pane_current_path}"
bind _ split-window -v -c "#{pane_current_path}"


             
set-window-option -g mode-keys vi

# vim style copy paste mode
bind-key -T copy-mode-vi 'v' send-keys -X begin-selection
bind-key -T copy-mode-vi 'V' send-keys -X select-line # line-wise selection
bind-key -T copy-mode-vi 'C-v' send-keys -X select-block # block selection
bind-key -T copy-mode-vi 'y' send-keys -X copy-selection-and-cancel

# Optional: remap paste to 'p' (default is ']')
unbind p
bind p paste-buffer

bind-key "T" run-shell "sesh connect \"$(
  sesh list --icons | fzf-tmux -p 80%,70% \
    --no-sort --ansi --border-label ' sesh ' --prompt '⚡  ' \
    --header '  ^a all ^t tmux ^g configs ^x zoxide ^d tmux kill ^f find' \
    --bind 'tab:down,btab:up' \
    --bind 'ctrl-a:change-prompt(⚡  )+reload(sesh list --icons)' \
    --bind 'ctrl-t:change-prompt(🪟  )+reload(sesh list -t --icons)' \
    --bind 'ctrl-g:change-prompt(⚙️  )+reload(sesh list -c --icons)' \
    --bind 'ctrl-x:change-prompt(📁  )+reload(sesh list -z --icons)' \
    --bind 'ctrl-f:change-prompt(🔎  )+reload(fd -H -d 2 -t d -E .Trash . ~)' \
    --bind 'ctrl-d:execute(tmux kill-session -t {2..})+change-prompt(⚡  )+reload(sesh list --icons)' \
    --preview-window 'right:55%' \
    --preview 'sesh preview {}'
)\""

# Reload Config 
unbind r
bind r source ~/.config/tmux/tmux.conf \; display "Reloaded!"

# Create new windo with custom name
bind-key C command-prompt -p "Name of new window: " "new-window -n '%%'"
